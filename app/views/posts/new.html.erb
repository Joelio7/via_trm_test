
<%= bootstrap_form_for(@post) do |f| %>
  <%= f.text_field :title %>
  <%= f.text_area :body %>
  <%= f.submit "Submit Post" %>
  <%= link_to "Cancel", posts_path, :method => "get", class: 'btn btn-warning' %>
<% end %>
